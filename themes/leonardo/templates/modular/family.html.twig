<div id="{{page.header.title}}" class="{{page.header.classs}}">
    <div class="content container mx-auto md:w-10/12 py-10 lg:py-12 text-center ">
        {{content|raw}}

        <div class="family-wrapper flex flex-col md:flex-row justify-center md:space-x-4 py-8">
            {% for member in page.header.family %}
                <div class="family-box rounded bg-white shadow-md my-4 md:my-0 md:w-3/12">
                    <div class="head">
                        {{ page.media[member.image].html('','','w-full h-48') }}
                    </div>
                    <div class="body p-3">
                        <h3 class="text-bold text-black">{{member.name}}</h3>
                        <span>{{member.role}}</span>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</div>